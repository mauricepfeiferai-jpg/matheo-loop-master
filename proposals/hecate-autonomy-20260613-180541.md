---
status: vorgeschlagen
loop: hecate-autonomy-20260613-180541
erstellt: durch hecate.loop_factory
---

# Loop-Proposal: hecate-autonomy-20260613-180541

**Zweck:** Autonomie-Vorschlag: Restart Loop Fix
**Schedule:** `einmalig`

## Implementierung (nach Freigabe, via safety.harness)

```bash
#!/bin/bash
# hecate-autonomy-20260613-180541 — Ledger-instrumentiert ab Geburt (kein Beweis = kein Erfolg)
LEDGER="python3 -m hecate.ledger"
cd /root/projects/loop-master
RID=$($LEDGER start hecate-autonomy-20260613-180541)
OUTPUT=$(mktemp /var/lib/loop-master/hecate-autonomy-20260613-180541.XXXX.out)
if python3 -m hecate.autonomy_loop apply hecate-autonomy-20260613-180541 > "$OUTPUT" 2>&1; then
    $LEDGER finish "$RID" --output "$OUTPUT"
else
    $LEDGER finish "$RID" --status failed --note "exit != 0"
fi
```

## Abnahme (vor Status: verifiziert)
1. Ein echter Lauf erzeugt einen ok-Eintrag: `python3 -m hecate.ledger report --loop hecate-autonomy-20260613-180541`
2. Provozierter Leerlauf landet als empty_output, nie als ok
3. sensors.ledger_stale meldet den Loop, wenn er >26h keinen ok-Lauf hat

## Rollback
Cron-Zeile entfernen. Keine weiteren Spuren (Ledger-Historie bleibt als Audit).

> Umsetzung NUR nach Freigabe und durch safety.harness.run() — Deny-List gilt.


## Autonom erzeugte Inhalte

**Zusammenfassung:**
Reduce restart spam

**Betroffener Sensor:** `restart_loops` (1 Befunde)

**Test-Datei:** `tests/test_restart_loops_improvement.py`

**Test-Code:**
```python
def test_restart_loops_improvement_has_baseline():
    # Baseline: Sensor restart_loops sollte nach Fix weniger krit/hoch Befunde melden
    assert True  # Platzhalter: wird nach GO durch echten Test ersetzt

```

**Modul-Code:**
```python
# Verbesserung fuer Sensor restart_loops: Reduce restart spam

```

**Status:** telegram_approval
